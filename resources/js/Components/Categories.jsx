import React from "react";
import Tags from "./Tags";

const Categories = ({ tags }) => {
    return (
        <div>
            <Tags tags={tags} />
        </div>
    );
};

export default Categories;
